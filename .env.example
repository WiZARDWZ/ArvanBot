# Example environment only. Do not place real credentials in this file.
ARVAN_SETTINGS_FILE=config/settings.example.yaml

# Store the complete Machine User key in your secret manager and inject it at runtime.
# Example placeholder only; keep provider write functionality disabled until live validation.
ARVAN_MU_KEY=apikey REPLACE-WITH-AUTHORIZED-SECRET
